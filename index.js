for (var i = 0; i < data.length; i++) {
    for (var x = 0; x < data.length; x++) {
        // console.log(data[i][x]);
        if (data[i][x]['value'] === null)
        console.log(data[i][x]);
    }
}